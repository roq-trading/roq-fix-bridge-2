.. tab:: Heartbeat (0)

   Either side must send this message as a response to a received :code:`TestRequest` (:code:`1`) message.

   .. list-table::
     :header-rows: 1
     :widths: 5 5 10 30 50

     * -
       -
       - Tag
       - Name
       - Comments

     * -
       -
       - 112
       - TestReqId
       - Value must be copied from a received :code:`TestRequest` (:code:`1`) message.
